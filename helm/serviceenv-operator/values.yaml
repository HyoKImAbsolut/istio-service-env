# 镜像配置
image:
  repository: ghcr.io/zaeyi/serviceenv-operator
  tag: ""  # 留空则使用 Chart.appVersion，也可通过 --set image.tag=xxx 覆盖
  pullPolicy: IfNotPresent

# 副本数
replicas: 1

# 资源限制
resources:
  limits:
    memory: 256Mi
    cpu: 200m
  requests:
    memory: 128Mi
    cpu: 50m
