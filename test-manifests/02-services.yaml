# Kubernetes Services
---
apiVersion: v1
kind: Service
metadata:
  name: httpbin
  namespace: serviceenv-test
spec:
  ports:
    - port: 8000
      name: http
      targetPort: 8080
  selector:
    app: httpbin
